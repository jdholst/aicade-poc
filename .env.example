OPENAI_MODEL=gpt-5.4-mini

# Optional: lets local QA use the keyword field instead of pasting an API key.
# Replace this placeholder in .env.local. Do not commit real keys.
KEYWORD_INTERNAL_TEST=sk-...
